code formating with black: python3 -m black main.py
pep8
good variable names: avoid short and long ones, avoid redundant, numeric suffixes
avoid duplicate code: use functionss, classes or loops instead
make CONSTANT variables to store default values and later update them if needed
don't forget commented out code, avoid 'dead code'
avoid print debugging, use logs instead
use list, set and dictionary comprehensions
empty exception blocks
avoid x1,x2,x3: use a single variable with a list of those

fdfdfdfs